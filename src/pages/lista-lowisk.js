import React, { Component } from "react";
import Filters from "../components/filteredFisheries";

function Lista({ data }) {
  return <Filters voivodeship="Wielkopolskie" />;
}

export default Lista;
